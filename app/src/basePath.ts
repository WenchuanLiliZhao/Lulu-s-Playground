// Page route path used by React Router.
// Route prefix (for BrowserRouter basename). Remove trailing slash to avoid double slashes.
export const basePath = '/jirareport';

// Static asset path used for the Vite base configuration.
export const assetsPath = '/jirareportres/';

// Production environment uses assetsPath as the base.
export const productionBase = '/jirareportres/';
