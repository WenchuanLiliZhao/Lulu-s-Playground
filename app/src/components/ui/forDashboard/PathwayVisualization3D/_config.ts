// Default configuration values for PathwayVisualization3D component
export const PATHWAY_VISUALIZATION_DEFAULTS = {
  gridSize: 20,
  gridCellSize: 2,
  flagHeight: 1.5,
  flagSize: 0.8,
  cameraDistance: 15,
  cameraElevation: 0.8, // Camera height factor (0-1), multiplied by cameraDistance
  autoRotate: true,
  autoRotateSpeed: 0.1,
  labelSize: 4,
  labelDistance: 0.4,
  iconSize: 0.5,
  lineWidth: 1,
  dashAnimationSpeed: 0.01,
  showAxes: false,
  showGrid: true,
}

