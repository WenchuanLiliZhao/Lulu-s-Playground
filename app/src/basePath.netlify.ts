// Page route path used by React Router for Netlify deployment.
// Route prefix (for BrowserRouter basename). Use root path for Netlify.
export const basePath = '/';

// Static asset path used for the Vite base configuration for Netlify.
export const assetsPath = '/';

// Production environment uses assetsPath as the base for Netlify.
export const productionBase = '/';

