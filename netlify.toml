[build]
  # Source code base directory
  base = "app"
  
  # Build command for Netlify (excludes jirareport paths)
  command = "npm run build:net"
  
  # Publish directory (relative to base)
  publish = "dist-net"

# Redirect all routes to index.html for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Optional: Set Node.js version
[build.environment]
  NODE_VERSION = "20"

